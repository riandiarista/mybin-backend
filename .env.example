# Environment variables example for XAMPP (MySQL)
# Copy this file to `.env` and fill values (do NOT commit real credentials).

# Server
PORT=3000

# JWT
JWT_SECRET=your_jwt_secret_here

# MySQL (XAMPP defaults)
DB_NAME=mybin
DB_USER=root
DB_PASS=
DB_HOST=127.0.0.1
DB_PORT=3306
